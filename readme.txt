Instructions to setup the project:

Open project in some IDE (e.g. IntelliJ), find pom.xml -> right click -> Add as Maven project.
After all dependencies are being install you should be able to run the project.
In src->test-> resource find testng.xml file -> right click -> click on run button.

Test report can be found in TestResult folder.